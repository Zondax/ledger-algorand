// The input public key should be 32 bytes long.
// The output buffer must be at least 65 bytes long.
#include "stdint.h"
// uint8_t convert_to_public_address(const uint8_t *publicKey, unsigned char *out);

// uint8_t convert_to_public_address2(const uint8_t *publicKey, unsigned char *out);
